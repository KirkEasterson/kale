---
- hosts: localhost
  connection: local

  pre_tasks:
    - include_tasks: roles/common/tasks/update_repos/pacman.yml

  tasks:
    - include_tasks: roles/arch/local.yml

  post_tasks:
    - include_tasks: roles/common/tasks/update_repos/pacman.yml
