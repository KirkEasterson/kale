---
- name: "Install theme tools"
  become: true
  vars:
    theme:
      Archlinux: "pop-gtk-theme"
  ansible.builtin.package:
    name:
      - "capitaine-cursors"
      - "lxappearance"
      - "papirus-icon-theme"
      - "picom"
      - "{{ theme[ansible_facts['os_family']] }}"
