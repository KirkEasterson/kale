---
- name: Check if downloads directory exists
  stat:
    path: $HOME/Downloads
  register: downloads_dir

- name: Create downloads directory
  become: true
  become_user: "{{ remote_regular_user }}"
  file:
    path: $HOME/Downloads/
    state: directory
  when: downloads_dir.exists == false
