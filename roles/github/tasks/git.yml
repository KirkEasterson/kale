---
- name: Install git and git tools
  become: true
  ansible.builtin.package:
    name:
      - "git"
      - "git-delta"
      - "difftastic"
      - "{{ gh.msg[ansible_facts['os_family']] }}"
  vars:
    gh:
      ArchLinux: github-cli
      Debian: gh
      Darwin: gh
